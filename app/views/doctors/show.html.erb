<h3>Doctor Show Page</h3>

Doctor Name: <%= @doctor.name %>
<br>
<br>
Department: <%= @doctor.department %>
<br>
<br>
Appointments:<br>
<br>
<ul><% @doctor.appointments.each do |appt| %>
    <li><%= link_to(appt.patient.name, patient_path(appt.patient)) %> : <%= link_to(appt.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(appt)) %></li>
    <% end %>
</ul>

