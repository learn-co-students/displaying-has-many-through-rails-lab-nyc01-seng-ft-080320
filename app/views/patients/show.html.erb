<%= @patient.name %><br>
<% @patient.appointments.each do |appointment| %>
  <% @patient_doctor = @patient.doctors.find(appointment.doctor_id) %>
  <%= link_to @patient_doctor.name, doctor_path(@patient_doctor) %> - <%= link_to appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(appointment) %><br>
<% end %>
    
    
    
    <%# @patient.name %>

    
    <%# @patient.appointments.each do |appt| %>
    <%# @patient_doctor = @patient.doctors.find(appt.doctor_id)%>
        <%# link_to @patient_doctor.name, doctor_path(@patient_doctor) %>
        <%# link_to appt.appointment_datetime.strftime("%B, %d, %Y, at %k:%M"), appointment_path(appt) %>
    <%# end %>