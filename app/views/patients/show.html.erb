<h3>Patient Show Page</h3>

Patient Name: <%= @patient.name %>
<br>
<br>
Age: <%= @patient.age %>
<br>
<br>
Appointments:<br>
<br>
<ul><% @patient.appointments.each do |appt| %>
    <li><%= link_to(appt.doctor.name, doctor_path(appt.doctor)) %> : <%= link_to(appt.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(appt)) %></li>
    <% end %>
</ul>